import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  actionArea: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  }
}));